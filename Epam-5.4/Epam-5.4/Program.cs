using Microsoft.VisualBasic.CompilerServices;
using System;
using System.Threading;

namespace Epam_5._4
{
    enum Color { Red = 4, Green = 23, Blue = 18}
    class Program
    {
        static void Main(string[] args)
        {
            int a = 24;
            Console.WriteLine(a);


            //t s = 
           //ng arSort = Enum.GetName(typeof(Color),
          //Console.WriteLine(arSort);
        }
    }
}
